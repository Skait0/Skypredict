{
  "name": "formline",
  "version": "1.0.0",
  "private": true,
  "description": "Football predictions, built and served from Vercel",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/prebuild.js",
    "test": "node --test"
  },
  "dependencies": {
    "@vercel/speed-insights": "^2.0.0"
  }
}
